Generated by 
<a href="{% if manifest.homepage is not empty %}
    {{- manifest.homepage -}}
{% endif %}" title="View the package online">{{ manifest.title }} {{ manifest.version }}</a>
by 
{% for author in manifest.authors %}
    {% set author_url='' %}
    {% set url_title='View its webpage online' %}
    {% if author.url is not empty %}
        {% set author_url=author.url %}
    {% elseif author.homepage is not empty %}
        {% set author_url=author.homepage %}
    {% elseif author.email is not empty %}
        {% set author_url='mailto:' ~ author.email %}
        {% set url_title='Contact this author' %}
    {% endif %}
    {%- if author_url is not empty -%}
        <a href="{{ author_url }}" title="{{ url_title }}">
    {%- endif -%}
        {{- author.name -}}
    {%- if author_url is not empty -%}
        </a>
    {%- endif -%}
    {% if loop.length > 1 and author.role is not empty %} (<em>{{- author.role -}}</em>){%- endif -%}
    {%- if loop.index < (loop.length) %}, {% endif %}
{% endfor %}
 under <a href="http://www.gnu.org/licenses/gpl-3.0.html" title="Read this license text online">GPL-3.0</a> license.
