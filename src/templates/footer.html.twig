<footer id="footer">
    <div class="muted credits pull-left">
        This page is created with <a href="http://jquery.com/" title="See online: http://jquery.com/">jQuery</a>, <a href="http://twitter.github.com/bootstrap/index.html" title="See online: http://twitter.github.com/bootstrap/index.html">Bootstrap</a> and <a href="http://code.google.com/p/html5shiv/" title="See online: http://code.google.com/p/html5shiv/">HTML5shiv</a>.
        <br />
        Icons by <a href="http://glyphicons.com/" title="See online: http://glyphicons.com/">Glyphicons</a>.
        <br />
        This page is <a href="" title="Check now online" id="html_validation" data-insert-href="html_validation">HTML5</a> & <a href="" title="Check now online" id="css_validation" data-insert-href="css_validation" data-validation-argument="perso/styles.css">CSS3</a> valid.
    </div>
    <div class="muted credits pull-right text-right">
        Generated by 
        <a href="{% if manifest.homepage is not empty %}
            {{- manifest.homepage -}}
        {% endif %}" title="View the package online">{{ manifest.title }} {{ manifest.version }}</a>
        by 
{% for author in manifest.authors %}
    {% set author_url='' %}
    {% set url_title='View its webpage online' %}
    {% if author.url is not empty %}
        {% set author_url=author.url %}
    {% elseif author.homepage is not empty %}
        {% set author_url=author.homepage %}
    {% elseif author.email is not empty %}
        {% set author_url='mailto:' ~ author.email %}
        {% set url_title='Contact this author' %}
    {% endif %}
    {%- if author_url is not empty -%}
        <a href="{{ author_url }}" title="{{ url_title }}">
    {%- endif -%}
        {{- author.name -}}
    {%- if author_url is not empty -%}
        </a>
    {%- endif -%}
    {% if loop.length > 1 and author.role is not empty %} (<em>{{- author.role -}}</em>){%- endif -%}
    {%- if loop.index < (loop.length) %}, {% endif %}
{% endfor %}
    under <a href="http://www.gnu.org/licenses/gpl-3.0.html" title="Read this license text online">GPL-3.0</a> license.
    <br />
{% if profiler is not empty %}
    <a href="#0" id="profiler_handler" title="See page &amp; system infos"><i class="icon-globe"></i>&nbsp;Profiler</a>
    &nbsp;|&nbsp;
{% endif %}
    <a href="{{ 'docbookdoc'|route }}" title="Read the manual"><i class="icon-question-sign"></i>&nbsp;Manual</a>
    &nbsp;|&nbsp;
    <a href="#0" onclick="messagebox('{{ 'credits'|route }}', 'About DocBook');" title="See the full application credits, check for updates ..."><i class="icon-info-sign"></i>&nbsp;About</a>
    </div>
{% if profiler is not empty %}
    <div id="profiler" class="profiler-wrapper">{% include DB.getTemplate('profiler') %}</div>
{% endif %}
</footer>
